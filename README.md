🚀 Стек технологий
Framework: Flutter (Material 3)

State Management: flutter_bloc

Navigation: auto_route (генерация путей)

Graphics: flutter_svg


🏗 Архитектура
Приложение построено по принципу Feature-First (разделение по фичам), что упрощает масштабирование и поддержку кода:

lib/core/ — конфигурация роутера, темы и глобальные настройки.

lib/features/ — функциональные модули (Home и Counter). Каждая папка содержит свой BLoC, события, состояния и UI-слой.

lib/shared/ — переиспользуемые компоненты (CustomLogo, NavigationBar).

💎 Особенности реализации
Интерактивный логотип: Логотип разделен на два независимых SVG-файла. Каждая часть является отдельной интерактивной зоной и меняет свой цвет независимо от другой.

Умная навигация: Реализован кастомный Navigation Bar. Кнопки меняют свое состояние (Коричневый — активна, Серый — неактивна) в зависимости от текущего маршрута на экране.

BLoC Logic: * В HomeBloc реализована логика переключения состояний цветов для каждой половины логотипа.

В CounterBloc реализован классический поток событий для инкремента счетчика.
Оптимизация ресурсов: Проведена ручная очистка SVG-файлов (удалены несовместимые с Flutter цветовые профили P3), что гарантирует корректную работу ColorFilter.

🛠 Как запустить
1. Подготовка
Убедитесь, что у вас установлен Flutter.

Bash
flutter pub get

2. Генерация кода
Проект использует генерацию кода для навигации. Выполните:

Bash
dart run build_runner build --delete-conflicting-outputs

3. Использование Makefile (если установлен)
Я подготовил Makefile для автоматизации рутины:

make setup — полная очистка и подготовка проекта к запуску.

make gen — запуск генератора кода.

make run — запуск приложения.

📂 Структура папок
Plaintext
lib/
├── core/
│   └── router/          # Конфигурация AutoRoute
├── features/
│   ├── counter/         # Логика и экран счетчика (BLoC + UI)
│   └── home/            # Логика и экран с логотипом (BLoC + UI)
└── shared/
    └── widgets/         # Общие компоненты (NavBar, LogoWidget)

Контакты

Буду рад обсудить технические решения и получить обратную связь!